# Phase 6 Customer physical-device checklist

**Target:** The next Staging internal previews are iOS build 38 and Android version code 37. These include the shared Development/Staging deletion fix, runtime environment validation, Firebase session synchronization, single-flight account bootstrap with cached-token validation and bounded claim refresh, SecureStore-compatible native operation IDs, sign-up password-focus repair, identity-scoped startup, the iOS root-overlay repair, deterministic cold-start session restoration, and the required iOS 27 scene lifecycle. Record their EAS build IDs here. Run every row on one physical iPhone and one physical Android device. The iPhone qualification target is iOS 27 after build 37 was terminated at launch by UIKit. Do not record passwords, tokens, addresses, or notification payload identifiers here.

| ID | Action | Expected result | iPhone | Android |
|---|---|---|---|---|
| AUTH-01 | Fresh install, register a new Customer, verify email, and return to the app. | The identity bootstraps once and enters the Customer app. | Registration/bootstrap passed on build 37. Retest the complete row on build 38 because build 37 was superseded by the cold-start restoration repair. | Retest required on version code 37 with a fresh disposable verified identity. |
| AUTH-02 | Close/reopen, sign out, sign in again, and restore the session once more. | Valid Customer state restores; logout removes private state and push ownership. | Retest on build 38. Build 37 restored Firebase authentication but remained indefinitely at “Preparing your secure session” after the app was closed and reopened. |  |
| AUTH-03 | Sign in with Restaurant and Admin test identities. | Each is signed out and shown the correct portal link; no Customer private data mounts. | Retest on build 38. The prior Admin attempt exposed Firebase's MFA error; the Customer app now reports generic incorrect credentials for that case. Restaurant wrong-portal routing remains to be verified. |  |
| AUTH-04 | Test suspended and revoked Customer identities, then restore test state. | Suspended shows support status with Support and Sign out actions; revoked signs out; direct data calls are denied. Suspension while the app is open is detected on foreground or within 15 seconds. | Partial: suspended startup state displayed correctly and an address mutation was denied. Retest the new actions, live suspension detection, revoked state, and direct order denial on build 38. |  |
| AUTH-05 | Delete a disposable Customer through the app, register the same email again, verify it, and sign in. Repeat once. | Each deletion removes the shared Firebase identity and releases both non-production database reservations; each new UID bootstraps without 400/401/403 responses. |  |  |
| DATA-01 | Browse restaurants and configured menus while signed out. | Public catalog, required/optional groups, prices, and removable ingredients display. |  |  |
| DATA-02 | Edit profile; create/update/default/delete an address; add/remove a favorite. | Every change persists after restart and belongs only to the current Customer. |  |  |
| DATA-03 | Open Notification preferences for a Customer with no existing preference row, change both Customer settings, save, close, and reopen. | The first open creates safe defaults without an error; saved values persist after reopening. Restaurant notification controls never appear. | Retest in the installed build. The database-only first-use correction is deployed to Staging and requires no replacement build. |  |
| CART-01 | Try missing required choices, too many choices, and a non-removable ingredient. | Incomplete or invalid configuration cannot enter the cart. |  |  |
| CART-02 | Add the same item with choices in different tap order, then add a genuinely different configuration. | Equivalent choices share one line; different choices remain separate lines. |  |  |
| ORDER-01 | Complete configured-menu checkout. Include separate lines of the same item with no customization, a paid extra, and a removed ingredient. | Server quote supplies displayed subtotal/fees/total; every configured line is preserved and one v2 order is created. | Device completion retest required. The Staging correction is deployed and a hosted quote for three Ateşli Burger lines—normal, extra meat, and no tomato—returned three lines and the authoritative total. This database-only correction does not require another app build. |  |
| ORDER-02 | Interrupt the response during submission and retry without changing the request. | The stable operation ID returns the same order; no duplicate order exists. |  |  |
| ORDER-03 | Observe the order through final state, open History/detail, and submit product/order reviews. Return to Home, reopen the same order, and also open a later order containing the same menu item. Background the app on the approval screen while Restaurant completes the order, then reopen it. | Current authoritative states and snapshots render; both review types persist. Home shows the updated restaurant rating/count without restarting the app. A menu item already reviewed in this order shows **Reviewed** and cannot be submitted again, including when the order contains multiple configured lines for that menu item. The same menu item remains reviewable in a different order. The Customer does not attempt deadline cancellation or show invalid-transition errors after the Restaurant has advanced the order. | Retest on the next iOS preview. The database already rejected duplicate reviews for the same order and menu item, but the app lost the order association when reviews were reloaded and showed Review again. The exact order-scoped lookup is deployed to Staging and the client now restores Reviewed state from it. A successful product review now invalidates cached rating aggregates, and Home reloads them on focus and foreground recovery. The waiting-screen race is also corrected: expiry is server-owned and status reconciliation opens order detail. |  |
| ORDER-04 | Have the Restaurant cancel a pending order using each supported reason and an internal staff note. Open the canceled order in the Customer Orders list and detail. | Both Customer screens show the translated cancellation reason. The Restaurant's internal note is never visible. Unknown or legacy reasons use a generic cancellation message. | Retest on build 38. The safe reason-code projection is deployed to Staging; database privacy coverage passes. |  |
| PUSH-01 | Register push, place an order, background the Customer app, and transition the order from Restaurant. Select the resulting notification. | Customer status notification opens the guarded detail page for that order and fetches its current authoritative state. Initial Customer order submission does not notify the same Customer; the Restaurant receives that new-order event. | Device retest required. The compatibility correction is deployed to Staging, and a rollback-only hosted probe created one event and one delivery for the affected Customer's active iOS token. Notification-tap routing now targets `/orders/<id>` directly instead of the Orders list and confirmation banner. |  |
| PUSH-02 | Sign out, trigger a former-account event, and select any retained notification. | No private data opens; server token ownership was revoked. |  |  |
| PUSH-03 | Deliver a Restaurant new-order/reminder payload to the Customer build. | The Customer app ignores it and never opens a privileged route. |  |  |
| SAFE-01 | Disable connectivity during startup and during an authenticated workflow, then reconnect. | Startup/data fail with retry; no Firestore data appears; recovery refetches Supabase. |  |  |
| SAFE-02 | Exercise the approved Supabase-outage simulation. | Customer data remains unavailable and no application-data fallback occurs. |  |  |
| SAFE-03 | Enable/disable Staging maintenance mode. | Maintenance blocks normal navigation and clears after policy restoration. |  |  |
| REL-01 | Start with the installed build allowed by Staging release policy. On iPhone, fully terminate and relaunch it on iOS 27. | Normal startup succeeds on API contract 2. The iOS build adopts the scene lifecycle and remains open instead of being terminated by UIKit. | Retest on build 38 — build 37 was superseded by the cold-start repair and then crashed at launch after the device was updated to iOS 27. Build 38 passed an Xcode 27 Release build and iOS 27 simulator launch/render check. | Retest on version code 37. |
| REL-02 | Raise the Staging minimum above the installed build. | Non-dismissible bilingual update screen appears and opens the correct platform store. |  |  |
| REL-03 | Restore the approved normal Staging policy and restart. | Update block clears and the normal Customer app returns. |  |  |

Phase 6 can be accepted only when every applicable cell passes on both physical platforms, hosted wrong-role/status probes pass, and Staging maintenance/release policy is restored to its reviewed normal state.
