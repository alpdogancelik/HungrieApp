# Phase 5 Restaurant desktop web/PWA review

**Status:** Development implementation and rehearsal completed on 2026-09-13. The app owner approved the exact Staging batch on 2026-09-13. The restricted backup was taken, the three checksum-pinned migrations and reviewed Realtime policy were applied, and the Staging Web Push sender was deployed. Restaurant web deployment and pilot owner/manager onboarding are complete. NAV-01–06, CAP-01–08, ORD-01–06, REC-01–06, PUSH-01–08, and CACHE-01–04 passed by pilot report and supporting technical probes by 2026-09-14. The staffed connected-screen readiness check passed. Exact device records and the app owner's explicit Phase 5 acceptance decision remain. No Production environment was touched, and legacy authorization remains live.

## Development result

The Restaurant proof app is replaced by a bilingual English/Turkish desktop application with login, invitation, pending and suspended states, dashboard, live orders, order detail, history, menu, Restaurant settings, reviews, alert settings, and security routes. Browser Firebase Auth uses session persistence and caller tokens for Supabase. Access routing uses `get_my_access_context_v1()`, while every private read or mutation independently checks canonical Restaurant state and scope.

The app initially fetches active orders, joins `restaurant-orders:v1:<restaurant-id>` as a private Realtime channel, and reconciles on invalidation, reconnect, focus, visibility, network restoration, and a configurable 15-second poll. It acknowledges an order only after rendering it. Opening order acceptance requires a successful initial context/sync, private channel join, and online state. Realtime and notification data are wake-up signals; the app always refetches authoritative state.

The menu UI supports categories, items, availability, accessible move-up/down ordering, JPEG/PNG/WebP upload, ingredients, option groups, selection limits, and price deltas through the v2 definition. `quote_order_v2` and `create_order_v2` accept IDs and quantities, validate selections and removable ingredients, price on the server, and store the definition revision and complete selection snapshot. Legacy menu fields and Customer RPCs remain unchanged.

Development was backed up at `secure/phase5-development-backup/2026-09-13T12-33-04-843Z/manifest.json`. Development had not received the accepted Phase 4 public type-boundary migration, so the checksum-pinned application batch correctly included that prerequisite followed by the three Phase 5 migrations. The applied batch SHA-256 is `4dbab69b9202fa8529ed3e1246e50f59c869ec091963ef1306e8f00357828238`. Development now has 35 migrations, 60 canonical accounts, 9 canonical Restaurant accounts, and all 9 legacy memberships remain present. No Phase 5 operation was fabricated during migration.

## Database and service contracts

The Staging Phase 5 migration batch is:

1. `20260913140000_phase5_restaurant_desktop.sql`
2. `20260913141000_phase5_restaurant_contracts.sql`
3. `20260913142000_phase5_customer_menu_v2.sql`

Combined SHA-256: `e912504aa523c32b090f8d7e323de86666b4228b9a0449602fe8d784c35eb631`.

The batch adds canonical Restaurant dashboard/order/history/detail/settings/review/menu/push RPCs; operation and order-visibility ledgers; versioned menu tables; scoped public Restaurant media; v2 Customer pricing/order creation; canonical private Realtime topic authorization; and FCM delivery claim/completion. Order transitions lock the row, validate expected version and deadline, require approved cancellation reasons, preserve idempotency, and audit changes. Existing legacy order topics and Expo recipient materialization remain active alongside the canonical path.

The Firebase source batch contains `dispatchRestaurantWebPushStaging`. It claims only canonical Web/FCM deliveries through service-only RPCs, sends a generic localized wake-up message without Customer data, records success/retry/dead-letter outcomes, and retires invalid registrations. Function source SHA-256: `8e873b7206454a54942b33a9131250c38b7ca3faca991fa618d2d6db6ddd97a2`.

The complete Restaurant route/source/service-worker/EAS configuration checksum, including the root lockfile and Admin invitation integration, is `7c855a518073122118601f0ce7dc216df7c54b80d9cd1912997705a8e1299ada`. The worker has no fetch handler or Cache API use. It displays generic background notifications and opens an authenticated order route, which refetches current state. Authenticated HTML uses `private, no-store`; CSP, HSTS, frame denial, referrer policy, permissions policy, and `nosniff` are configured.

## Verification

- Clean local database reset applied all 35 migrations.
- All 612 database assertions passed, including legacy notification and Realtime compatibility.
- Courier, Admin-role, and invitation concurrency tests passed.
- Database lint returned no errors.
- Phase 5 Firebase messaging unit tests passed.
- All JavaScript repository tests passed.
- Restaurant and Admin TypeScript checks passed.
- Shared packages, Restaurant web export, and Admin production build passed.
- Restaurant export produced all 16 static routes.
- Route/security inspection found 13 required product routes, seven required security headers, no service-worker cache handler, and correct Restaurant invitation portal selection.

## Exact Staging review bundle

Read-only review on 2026-09-13 found 32 applied Staging migrations and exactly the three Phase 5 migrations pending. Staging currently has 14 profiles, 14 canonical accounts, and 11 canonical Restaurant accounts. The managed Realtime policy SHA-256 is `11d60aebcfde9ee2656afd3ad2efa0bb7ecbc4929d9d0aca79641067e9eb3815`. Restricted review record: `secure/phase5-staging/review-1789303276365.json`.

The app owner approved this exact Staging batch on 2026-09-13 and supplied the pilot owner and manager addresses out of band. The addresses are not persisted in this review. Execution evidence so far:

- Restricted backup: `secure/phase5-staging-backup/2026-09-13T12-49-08-704Z/manifest.json`.
- Backup schema SHA-256: `0b975e548ac7575bf6e2b59d636645c0a811bca4369ac4e21af71df63b53e409`.
- Backup data SHA-256: `0ea2ad34e9f42d4a9884714e7fcbb07fc47f4edce68a8b743089a40ed5f74c63`.
- The migration dry-run passed, and only the three reviewed Phase 5 migrations were applied in order.
- Hosted verification found 35 applied migrations, 14 canonical accounts, 11 canonical Restaurant accounts, nine unchanged legacy memberships, an authenticated canonical dashboard RPC, private delivery ledgers, service-only delivery claims, and one canonical Realtime policy.
- The reviewed receive-only Realtime policy was applied with SHA-256 `11d60aebcfde9ee2656afd3ad2efa0bb7ecbc4929d9d0aca79641067e9eb3815`.
- `dispatchRestaurantWebPushStaging` was deployed successfully in `us-central1` with its one-minute scheduler and the existing Staging Supabase secrets. No secret value was recorded in this review.
- The dedicated non-production VAPID public key and the remaining public Firebase/Supabase browser configuration were added to the EAS Preview environment. The VAPID value is intentionally omitted here.
- The first EAS upload exposed a deployment-procedure defect: `eas deploy --environment preview` did not inject Preview values into the preceding local Expo export. That deployment was never used for a pilot. The deployment script now runs a cache-cleared export through `eas env:exec preview` and refuses an export that lacks the reviewed Firebase project, VAPID key, or Supabase URL.
- The corrected EAS deployment is `https://hungrie-restaurant--dzcrems18v.expo.app`, assigned to the Staging alias `https://hungrie-restaurant--staging.expo.app`. Hosted inspection confirms the real Staging Firebase project, VAPID key, and Supabase URL are in the bundle, `/sw.js` is available, and the worker has no fetch handler or Cache API use.
- The Staging Admin portal was configured to generate Restaurant invitation links for the EAS Staging alias and redeployed as Vercel deployment `dpl_6qAVaAHNwkkuZaxKg5Y9FiwpN18o`, retaining `https://hungrie-admin-web-phase1.vercel.app` as its alias.
- Hosted Restaurant HTML is `private, no-store`, includes CSP `frame-ancestors 'none'`, HSTS, `nosniff`, referrer policy, and permissions policy. EAS Hosting did not emit the configured legacy `X-Frame-Options` response header; the effective CSP frame denial remains present. This hosting behavior is recorded for review rather than weakening the CSP.
- Pilot owner enrollment exposed a static route and access-gate defect: `/invite/<token>` returned 404 on EAS Hosting, and an unmapped Firebase identity was routed away before its invitation could be accepted. A read-only Staging check confirmed one pending, zero accepted invitations and zero pending Restaurant accounts for the pilot owner after Firebase email verification. The fixed Admin flow issues `/invite?token=<one-time-token>` URLs; Restaurant serves `/invite` as a static page and allows unmapped invitees to reach its caller-bound acceptance RPC. The login page now shows the same generic sign-in error when redirected with `reason=failed`. EAS deployment `https://hungrie-restaurant--q3bmbts859.expo.app` is assigned to the Staging alias; Admin deployment `dpl_GQYLa1rD3nJv2WM9t19C5cwdFAKP` generates the corrected link format. The hosted `/invite?token=invalid-test-only` route returns 200 with `private, no-store`; `/invite/not-a-real-token` returns 404. No real invitation token was used in this check. The owner must be re-invited through Admin to invalidate the original link exposed in the support screenshot, then accept using **Existing account** with the already verified Firebase identity. No account activation occurred during this correction.
- The replacement owner invitation was accepted, leaving one mapped, pending Restaurant owner on a pending Restaurant. An attempted Admin activation did not commit an account-status operation. The callable log confirms valid Firebase authentication and records the attempt with an operation ID, but the current server log categorizes its underlying error as unknown. The Admin UI now uses that exact operation ID as its safe error reference and shows a targeted retry instruction; deployment `dpl_4gxCW16MUfQj2ihRAPWdGKcPfxpe` is live at the unchanged Admin alias. The activation itself remains unverified; the owner and Restaurant remain pending until a successful Admin transition.
- A later read-only Staging check confirmed the pilot owner account and Restaurant are both active, with `accepting_orders=false`. The manager account and invitation are not yet present. The earlier failed activation remains recorded as a diagnostic incident; its cause was not established from the available server log.
- The app owner subsequently confirmed that the pilot owner and manager both signed in. A read-only Staging check found both accounts active on the same active Restaurant, with order acceptance still off; the manager's canonical private-topic predicate returned true. The reported persistent navigation back to Dashboard came from `AuthGate` resetting to its loading state on every pathname change, unmounting the Expo Router Stack and restarting at the index redirect. The gate now keeps an already verified navigator mounted during route changes, while the existing access-context checks and protected RPC authorization remain in place. Restaurant TypeScript and all 16 static web routes exported successfully. The checksum-pinned web bundle `b65f04fb848d2e75a55059898fade116e29bbd20fbbc772ce2b61464c027ee78` was deployed as `https://hungrie-restaurant--s6x7mlyc4w.expo.app` and assigned to the unchanged Staging alias. Hosted `/orders` and `/menu` returned 200 with private no-store HTML and the configured CSP/security headers. Signed-in browser navigation and order delivery still require pilot retesting; the hosted response check alone does not close the issue.
- Pilot review also exposed raw JSON on an empty History page. The diagnostic rendering was replaced with localized English/Turkish empty states and a paginated history table containing order detail links, Customer, status, total, and localized date fields. Restaurant TypeScript and the 16-route export passed. Checksum-pinned bundle `b12d810fb447e71ec4333c36427ae6dc6419ae3fbbe9779fab3e01e75c5f7c55` was deployed as `https://hungrie-restaurant--81tjw4txwn.expo.app` under the unchanged Staging alias. An empty history remains expected for a new pilot Restaurant; raw API JSON is no longer presented.
- A long menu-item form exposed a document overflow defect: Expo's full-screen `ScrollViewStyleReset` locked the browser body with `overflow:hidden`, although this app uses normal document scrolling. The reset was removed and the Restaurant root now explicitly permits vertical document scrolling while preventing horizontal page overflow. TypeScript and the 16-route export passed. Checksum-pinned bundle `806a3b9a45b1e45c97071ac34f9bfde2cfb016418f6ece3480675cf3c6cb6961` was deployed as `https://hungrie-restaurant--k2wecdtpci.expo.app` and assigned to the unchanged Staging alias. Hosted inspection confirms the current alias has no Expo overflow-lock style and its stylesheet enables vertical scrolling. Pilot browser confirmation with a long form remains required.
- The first pilot menu-item save with an image exposed a Storage RLS defect. The media policies called `private.current_restaurant_id()`, while `authenticated` correctly has neither private-schema usage nor execute permission on that helper, so every browser upload failed before the menu RPC ran. Additive migration `20260913143000_phase5_restaurant_media_policy_fix.sql` replaces that dependency with a narrow public boolean predicate that permits only an object below the caller's active canonical Restaurant folder. The private helper remains inaccessible. The four-file Phase 5 migration checksum is `74e09795520d2ecad13541f77b5945fec9210049341cac16c7db59aef14a44bb`; the release pipeline applied only the pending correction to Staging using the existing restricted backup. Local reset, 30 Phase 5 assertions, database lint, and a rolled-back Staging insert under an authenticated Restaurant claim passed; another Restaurant folder was denied. The form now validates option counts and non-negative prices before upload and distinguishes invalid media, upload failure, and menu-save failure with support-safe references. Restaurant TypeScript and the 16-route export passed. Checksum-pinned bundle `90cce91e2fea712cccbec0871b2d8f235d0828d900ef12cea2019589983a79f1` was deployed as `https://hungrie-restaurant--1rndsl6ol2.expo.app` under the unchanged Staging alias; hosted inspection found the new error handling in the served bundle. Owner and manager browser saves still require pilot confirmation.
- Pilot notification setup showed `✓ FCM Web Push` together with a registration error because the card treated browser permission as completed server registration. Staging contained no successful Restaurant Web/FCM token or registration operation. A clean Chrome probe against the deployed configuration, VAPID key, and service worker produced an FCM token; a rolled-back authenticated Restaurant probe passed `restaurant_register_web_push_v1`, isolating the remaining observation to the browser-to-registration sequence. The card now reconciles automatically when permission is already granted, shows the check mark only after the guarded RPC succeeds, distinguishes FCM token creation from Supabase registration failures, and checks unregister errors. TypeScript and all 16 routes exported successfully. Checksum-pinned bundle `7b028bc86db93a3afac50547bf1272b06da7181362186de87d80be9dfb3e34db` was deployed as `https://hungrie-restaurant--3dhmkya0rc.expo.app` under the unchanged Staging alias. Hosted inspection confirms the corrected status and diagnostic messages are served. A signed-in pilot retry remains required to confirm which path the original browser takes and to complete token registration.
- On 2026-09-13, the app owner confirmed that NAV-01 through NAV-06 in the Phase 5 pilot checklist passed for both pilot owner and manager accounts. This covers signed-in route navigation, direct route refreshes, language switching, readable order/history presentation, long-form scrolling, and sign-out/private-route protection. Device and browser details remain to be recorded separately.
- On 2026-09-13, the app owner confirmed that CAP-01 through CAP-07 passed for both pilot owner and manager accounts. The checksum-pinned CAP-08 technical probe then created a transient active canonical Restaurant manager, attempted `restaurant_bulk_set_item_availability_v1` against another Restaurant's item, and received HTTP 403 with PostgreSQL code `42501`. The target item's tenant, availability, definition revision, and update timestamp were unchanged; no Restaurant operation or audit row was created. Database and Firebase cleanup passed, including removal of one transient Firebase probe identity retained when the first cleanup attempt encountered an expired operator token. Probe source SHA-256: `058cd1a3d81170727d3c57de31a04d2191e53cb32cd1f669064b482523e58758`.
- During ORD-04 on 2026-09-13, the app owner clicked Preparing in Chrome and then in Brave. One browser became stuck on Restaurant access checking and later displayed a service-unavailable gate. A read-only Staging inspection found the affected order in `preparing`, with exactly one preparing history event and one transition audit event; the database responded and had no lock waiters or inactive Restaurant accounts at inspection time. The exact cause of the transient access-context failure was not established. The Restaurant client now preserves an already verified session's operational shell if a later context request fails, resets verification when the Firebase identity changes, prevents a second local transition while one is running, and refreshes the losing session's order while retaining the order-changed message. Restaurant TypeScript, Phase 5 route/security checks, and the 16-route web export passed. Reviewed source SHA-256 `617f099e9393b57b141809060f5a80486502bbebdd7b038f4661906ae0378b98` deployed to `https://hungrie-restaurant--ehgufrzcmb.expo.app` at the unchanged Staging alias. Hosted `/orders` returned 200 with private/no-store headers and the exact exported entry bundle SHA-256 `43e88499513d233bf7c7f1175d831a59c0fcbe970283637e42ddc249a479befb`. ORD-04 remains open for a new simultaneous two-browser pilot test; the reported Chrome/Brave attempt does not count as passed.
- The app owner reported that the Staging website still remained on the Restaurant access-checking screen after that deployment. The root gate could still hide the Expo Router Stack while requesting a redirect, preventing navigation from completing. The follow-up frontend change keeps the navigator mounted behind an opaque access overlay, bounds each context request to 12 seconds, and turns a stalled initial check into a Retry state after 15 seconds. Protected RPCs and RLS remain the authorization boundary. Restaurant TypeScript, route/security checks, and the 16-route web export passed. Source SHA-256 `da62bcbfd8813d80ecd5e8d5ca7e2893181e31cb228d23882fd97f685762464c` deployed to `https://hungrie-restaurant--0rkesqss8q.expo.app` under the unchanged Staging alias. Hosted `/orders` returned 200 with private/no-store HTML; its entry asset exactly matched the exported bundle SHA-256 `0ae609c03520e1e5d43f2253bb23d1eef4dea43b191aa8c86e2221a6b44391b0`, and the access-overlay CSS was present. Signed-in Chrome/Brave recovery and ORD-04 still require pilot confirmation; hosted verification does not count as a pass.

Remaining Staging sequence:

The first REC-01 attempt produced a generic service error after the network was disconnected, without clearly saying that the device was offline. The authenticated Restaurant shell now listens for browser online/offline events on every route and shows a persistent, assertive English/Turkish warning that live orders cannot update and staff must reconnect before responding. The checksum-pinned build `05d8d79eebb5ac46517ee223d23e5d73e43ecd6517cd3285340840add2444bc2` was deployed to `https://hungrie-restaurant--44u7lrqpgh.expo.app` under the unchanged Staging alias. Hosted `/orders` returned 200, its bundle matched the tested export, and the bundle contains the offline listener and warning. Staging API, Realtime, expiry, and notification health passed. REC-01 remained open pending pilot confirmation at that point.

The app owner reported REC-01–06 behavior passing on 2026-09-14. An initial read-only cleanup check found the pilot manager account restored from suspended to active and both pilot owner and manager accounts active, while the Restaurant entity remained suspended. A later read-only check confirmed the Restaurant was restored to active with order acceptance off. REC-06 behavior and cleanup are therefore passed.

The app owner reported PUSH-01–08 passing on 2026-09-14 for all four required desktop combinations: Windows Chrome, Windows Edge, macOS Chrome, and macOS Edge. This covers registration, foreground test and order alerts, background and closed-page delivery, permission denial and revocation, authenticated notification routing, generic payload privacy, and duplicate-alert suppression. The exact device models, OS/browser versions, PWA installation state, permission state, tester, and date still need to be entered in the checklist's device record.

Authenticated automated cache/privacy probes ran against the live Staging alias on macOS 26.3 with Chrome 153.0.8010.36 and Edge 153.0.4234.32 on 2026-09-14. In both browsers, the temporary account signed in and reached the Restaurant shell. The service worker was activated and controlling the page, and `caches.keys()` remained empty before and after sign-out. The authenticated `/history` and `/orders` documents returned `private, no-cache, no-store`; each browser observed 11 dynamic Supabase responses, and none came from disk cache or the service worker. The temporary account had no order-detail link in History, so the offline portion revisited `/orders`: after sign-out, browser networking was disabled and the revisit produced `ERR_INTERNET_DISCONNECTED` without exposing the Restaurant shell or private data. In each browser, a generic service-worker notification containing only the standard wake-up body was created, and a synthetic notification-click event ended at Login. The repository guard also confirmed zero service-worker fetch/cache handlers. Windows Chrome/Edge confirmation remains for CACHE-01–03. CACHE-04 still requires a physical operating-system notification click on all four supported combinations; synthetic events do not close that requirement.

The app owner subsequently reported that the remaining NAV-02 static-route retest and CACHE-01–04 physical checks all passed on Windows Chrome/Edge and macOS Chrome/Edge on 2026-09-14. This closes the remaining functional browser rows, including physical notification clicks after sign-out. Staffed connected-screen readiness was also reported as passed. Exact device, OS, browser, PWA installation, notification-permission, tester, and date fields still need to be transcribed into the checklist's device record before acceptance evidence is complete.

The 2026-09-14 ORD-04 retest initially kept the site available, but the second browser gave no changed-status explanation and left Cancel disabled while waiting. The order detail now bounds transition and refresh requests, unlocks controls in `finally`, reconciles on focus/visibility/network return and polling, and displays the server's current status when its version changes. Hosted verification exposed an additional static-hosting issue: a cold `/orders/<id>` request returned 404, so a push click or direct refresh could fail. The canonical order-detail link is now `/orders/detail?orderId=<id>`; the fixed route is statically exported, and all Restaurant list, history, foreground/background push, and Staging sender links use it. The original dynamic route remains for in-app compatibility. The checksum-pinned web build `165ac7e97bb3fb35c7cfef5bcf931571919f5c7ca7d6543a5683d613484a89de` was deployed to `https://hungrie-restaurant--maj968ptp0.expo.app` under the unchanged Staging alias, and the Staging Firebase sender route hint was deployed. A hosted cold request to the new detail URL returned 200 with private/no-store HTML; its entry asset matched the exported bundle. Restaurant TypeScript, sender tests, and Staging API/Realtime/expiry/notification health passed. The app owner subsequently reported ORD-01–06 passing, including the ORD-04 retest. NAV-02 still requires owner/manager confirmation on the new static detail URL.

On 2026-09-13, the ORD-04 pilot interruption exposed two separate faults. The generated Expo HTML included `globalThis.__EXPO_ROUTER_HYDRATE__=true;`, but the Restaurant CSP blocked that inline script. The Staging Supabase REST service also returned `PGRST003`/504 (PostgREST connection-pool acquisition timeout) across Restaurant, Customer, and notification-worker RPCs; the browser displayed a secondary CORS error because the 504 gateway response lacked CORS headers. Firebase Auth and direct database management queries remained responsive. A read-only check found zero pending orders, one preparing order, and no lock waiters. The Staging project was restarted to recover REST availability, and the Restaurant CSP now permits only the exact SHA-256 of Expo's generated inline script. The Staging deploy command checks every exported HTML page against that CSP before publishing. Hosted `/orders` confirms the hash is present in the served CSP. After the restart, the project reported `ACTIVE_HEALTHY`, REST health returned HTTP 200 with the expected CORS header, guarded unauthenticated RPCs returned HTTP 401 with CORS headers, and the Staging API/Realtime/expiry/notification health check passed. The Staging web deployment at that point was `https://hungrie-restaurant--tubidcj65g.expo.app` under the unchanged alias. ORD-04 and signed-in recovery were still awaiting pilot retest at that point.

Use the executable [Phase 5 pilot test checklist](phase-5-pilot-test-checklist.md) to record the required browser, device, workflow, notification, recovery, and cache evidence.

1. Record Windows and Mac device models, OS versions, Chrome/Edge versions, PWA installation state, permission state, tester, and test date.
2. Record the app owner's explicit Phase 5 acceptance decision in the checklist.

Browser/device qualification remains required for the Phase 5 exit gate. Development and Staging deployment do not qualify Phase 5 for acceptance.

## Rollback

If a migration fails, stop before function or web deployment and use the restricted backup only when a forward fix cannot safely restore migration history. If the sender fails, leave delivery rows retryable/dead-lettered and remove only `dispatchRestaurantWebPushStaging`; never expose credentials or relax canonical guards. If the web build fails qualification, remove the Staging alias/update and keep Restaurant order acceptance off for pilot identities. Legacy clients, topics, tables, and RPCs remain available throughout Phase 5, so rollback must not weaken authorization.
