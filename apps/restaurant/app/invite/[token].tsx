import{FormEvent,useState}from"react";import{createUserWithEmailAndPassword,sendEmailVerification,signInWithEmailAndPassword}from"firebase/auth";import{useLocalSearchParams,useRouter}from"expo-router";import{auth,ensureSessionPersistence}from"../../src/firebase";import{supabase}from"../../src/supabase";import{useLocale}from"../../src/providers";
export default function Invite(){const{token}=useLocalSearchParams<{token:string}>(),router=useRouter(),{t}=useLocale();const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[verify,setVerify]=useState(false),[mode,setMode]=useState<"create"|"signin">("create");async function submit(e:FormEvent){e.preventDefault();setError("");try{await ensureSessionPersistence();const user=mode==="create"?(await createUserWithEmailAndPassword(auth,email,password)).user:(await signInWithEmailAndPassword(auth,email,password)).user;if(!user.emailVerified){await sendEmailVerification(user);setVerify(true);return}await user.getIdToken(true);const result=await supabase.rpc("accept_my_account_invitation_v1",{p_token:String(token),p_operation_id:crypto.randomUUID()});if(result.error)throw result.error;router.replace("/pending")}catch{setError(t.invalid)}}return <main className="center"><form className="card" onSubmit={submit}><h1>{t.invite}</h1>{verify?<p>{t.verify}</p>:<><div className="row"><button type="button" className={mode==="create"?"button":""} onClick={()=>setMode("create")}>New account / Yeni hesap</button><button type="button" className={mode==="signin"?"button":""} onClick={()=>setMode("signin")}>Existing account / Mevcut hesap</button></div><label className="field">{t.email}<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="field">{t.password}<input type="password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<p className="danger">{error}</p>}<button className="button">{mode==="create"?t.create:t.signIn}</button></>}</form></main>}
