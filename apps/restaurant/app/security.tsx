import{sendPasswordResetEmail,signOut}from"firebase/auth";import{Shell}from"../src/Shell";import{auth}from"../src/firebase";import{useLocale}from"../src/providers";export default function Page(){const{t}=useLocale();return <Shell><section className="card"><h1>{t.security}</h1><p>{auth.currentUser?.email}</p><button onClick={()=>auth.currentUser?.email&&void sendPasswordResetEmail(auth,auth.currentUser.email)}>Reset password</button> <button onClick={()=>void signOut(auth)}>{t.logout}</button></section></Shell>}
