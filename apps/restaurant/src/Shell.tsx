import{Link}from"expo-router";import{signOut}from"firebase/auth";import{auth}from"./firebase";import{useLocale}from"./providers";import{unregisterRestaurantPush}from"./push";
export function Shell({children}:{children:React.ReactNode}){const{locale,setLocale,t}=useLocale();const links:[[string,string],...Array<[string,string]>]=[["/dashboard",t.dashboard],["/orders",t.orders],["/history",t.history],["/menu",t.menu],["/restaurant",t.restaurant],["/reviews",t.reviews],["/settings",t.settings],["/security",t.security]];async function logout(){await unregisterRestaurantPush().catch(()=>undefined);await signOut(auth)}return <div className="shell"><nav className="nav"><h2>Hungrie</h2>{links.map(([href,label])=><Link key={href} href={href as any}>{label}</Link>)}<button onClick={()=>setLocale(locale==="en"?"tr":"en")}>{locale.toUpperCase()}</button><button onClick={()=>void logout()}>{t.logout}</button></nav><main className="content">{children}</main></div>}
