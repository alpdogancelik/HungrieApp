import{useCallback,useEffect,useRef,useState}from"react";import{Link}from"expo-router";import{Shell}from"./Shell";import{supabase}from"./supabase";import{useLocale}from"./providers";import type{OrderPage}from"./contracts";
const poll=Math.min(60000,Math.max(10000,Number(process.env.EXPO_PUBLIC_RESTAURANT_POLL_MS||15000)));
export function OrdersPage(){const{t}=useLocale();const[orders,setOrders]=useState<Record<string,any>[]>([]),[connected,setConnected]=useState(false),[online,setOnline]=useState(typeof navigator==="undefined"?true:navigator.onLine),[error,setError]=useState("");const restaurant=useRef("");const load=useCallback(async()=>{const result=await supabase.rpc("restaurant_list_orders_v1" as any,{p_queue:"active",p_cursor:null,p_limit:50});if(result.error){setError(t.unavailable);return}const items=(result.data as unknown as OrderPage)?.items||[];setOrders(previous=>{const versions=new Map(previous.map(x=>[x.id,x.updated_at]));return items.map(x=>versions.get(x.id)===x.updated_at?previous.find(y=>y.id===x.id)||x:x)});setError("")},[t.unavailable]);
useEffect(()=>{let timer:ReturnType<typeof setInterval>;let channel:any;void(async()=>{const dashboard=await supabase.rpc("restaurant_get_dashboard_v1" as any);restaurant.current=String((dashboard.data as any)?.restaurantId||"");await load();if(restaurant.current){await supabase.realtime.setAuth();channel=supabase.channel(`restaurant-orders:v1:${restaurant.current}`,{config:{private:true}}).on("broadcast",{event:"order_changed"},()=>void load()).subscribe((status:string)=>setConnected(status==="SUBSCRIBED"));}timer=setInterval(()=>{if(document.visibilityState==="visible"&&navigator.onLine)void load()},poll)})();const focus=()=>void load(),network=()=>{setOnline(navigator.onLine);if(navigator.onLine)void load()};window.addEventListener("focus",focus);window.addEventListener("online",network);window.addEventListener("offline",network);document.addEventListener("visibilitychange",focus);return()=>{clearInterval(timer);if(channel)void supabase.removeChannel(channel);window.removeEventListener("focus",focus);window.removeEventListener("online",network);window.removeEventListener("offline",network);document.removeEventListener("visibilitychange",focus)}},[load]);
useEffect(()=>{orders.forEach(order=>{if(order.updated_at)void supabase.rpc("restaurant_acknowledge_order_seen_v1" as any,{p_order_id:order.id,p_order_version:order.updated_at,p_operation_id:crypto.randomUUID()})})},[orders]);return <Shell><div className={`banner ${connected&&online?"healthy":""}`}><strong>{t.connection}:</strong> {connected&&online?"✓":"⚠"}</div><div className="toolbar"><h1>{t.orders}</h1><button onClick={()=>void load()}>{t.refresh}</button></div>{error&&<p className="danger">{error}</p>}<div className="grid">{orders.map(order=><article className="card" key={order.id}><h3>#{String(order.id).slice(0,8)}</h3><p>{order.customer_name||order.customer?.name||"Customer"}</p><p>{order.status} · {order.total_kurus!=null?`₺${(Number(order.total_kurus)/100).toFixed(2)}`:""}</p><Link href={`/orders/${order.id}` as any}>{t.open}</Link></article>)}</div>{!orders.length&&!error&&<p>{t.noRows}</p>}</Shell>}
