export const dynamic = "force-dynamic";
export const fetchCache = "force-no-store";

export default function PrivateProof() {
  return (
    <main style={{ maxWidth: 720, margin: "15vh auto", padding: 32 }}>
      <h1>Private route placeholder</h1>
      <p>No private query or authorization has been added in Phase 1.</p>
    </main>
  );
}
