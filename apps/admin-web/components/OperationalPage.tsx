"use client";
import{useCallback,useEffect,useState}from"react";import{supabase}from"@/lib/supabase";import type{PageResult}from"@/lib/contracts";import{useLocale}from"./AdminProviders";import{AdminControls}from"./AdminControls";
const rpc={restaurants:"admin_list_restaurants_v1",accounts:"admin_list_accounts_v1",orders:"admin_list_orders_v1",incidents:"admin_list_incidents_v1",audit:"admin_list_audit_v1"} as const;
export function OperationalPage({kind}:{kind:keyof typeof rpc}){const{t}=useLocale();const[page,setPage]=useState<PageResult>({items:[],total:0,limit:25,offset:0}),[error,setError]=useState(""),[search,setSearch]=useState("");const load=useCallback(async(offset=0)=>{setError("");const args:any={p_limit:25,p_offset:offset};if((kind==="restaurants"||kind==="accounts")&&search)args.p_search=search;const{data,error}=await supabase.rpc(rpc[kind] as any,args);if(error)setError(`${t.unavailable} Reference: ${crypto.randomUUID().slice(0,8)}`);else setPage(data as unknown as PageResult)},[kind,search,t.unavailable]);useEffect(()=>{void load(0)},[load]);const cols=page.items[0]?Object.keys(page.items[0]).filter(k=>!["metadata","threshold_snapshot","delivery_address"].includes(k)).slice(0,8):[];return <section><header className="page-header"><div><p className="eyebrow">Hungrie</p><h2>{t[kind]}</h2></div>{(kind==="restaurants"||kind==="accounts")&&<form onSubmit={e=>{e.preventDefault();void load(0)}}><input aria-label={t.search} value={search} onChange={e=>setSearch(e.target.value)} placeholder={t.search}/><button>{t.search}</button></form>}</header>{kind!=="audit"&&<AdminControls kind={kind} onDone={()=>void load(page.offset)}/>} {error&&<p className="error">{error}</p>}<div className="table-wrap"><table><thead><tr>{cols.map(c=><th key={c}>{c.replaceAll("_"," ")}</th>)}</tr></thead><tbody>{page.items.map((row,i)=><tr key={String(row.id??row.profile_id??i)}>{cols.map(c=><td key={c}>{row[c]==null?"—":typeof row[c]==="object"?JSON.stringify(row[c]):String(row[c])}</td>)}</tr>)}</tbody></table>{!page.items.length&&<p className="empty">{t.noRows}</p>}</div><footer className="pagination"><button disabled={!page.offset} onClick={()=>void load(Math.max(0,page.offset-page.limit))}>{t.previous}</button><span>{page.total?page.offset+1:0}–{Math.min(page.total,page.offset+page.limit)} / {page.total}</span><button disabled={page.offset+page.limit>=page.total} onClick={()=>void load(page.offset+page.limit)}>{t.next}</button></footer></section>}
